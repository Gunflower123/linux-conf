#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
#PS1='[\u@\h \W]\$ '

# Add scripts dir to PATH environment variable
export PATH="$PATH:$HOME/.local/bin"
